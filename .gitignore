node_modules/
dist/
data/
.env
logs/
*.log
.DS_Store

# Legacy: local scraper-probe dumps (now written to logs/, kept for safety)
probe-*.html
probe-*.json

# Tauri / Rust build output
app/src-tauri/target/
app/src-tauri/gen/
app/dev-sidecar/target/

# Sidecar binaries (built from source, not tracked)
app/src-tauri/binaries/*.exe
app/src-tauri/binaries/*.bat

# Design reference dump (pulled from Claude Design)
.design-ref/

# Schedule config (created at runtime in dev, lives in config dir)
schedule.json
scheduler-state.json

# Runtime backups dir (created by the Export button in Settings)
backups/

# Claude Code local cache
.claude/

# Pre-Tauri scripts kept locally for reference only (Windows Task Scheduler era)
.legacy/
